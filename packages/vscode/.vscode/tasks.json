{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Editor Stack",
      "type": "shell",
      "command": "npm run build && npm run build --prefix packages/language-server && node packages/language-server/scripts/bundle.mjs && npm run build --prefix packages/vscode",
      "options": { "cwd": "${workspaceFolder}/../.." },
      "group": "build"
    }
  ]
}
